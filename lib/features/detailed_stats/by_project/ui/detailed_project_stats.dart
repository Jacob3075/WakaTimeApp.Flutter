import "package:flutter/material.dart";

class DetailedProjectStats extends StatelessWidget {
  const DetailedProjectStats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
