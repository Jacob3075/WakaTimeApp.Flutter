import "package:flutter/material.dart";

class StatsPager extends StatelessWidget {
  const StatsPager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
